import React from 'react';
import { Icon } from '@iconify/react';

const QuestionIcon = ({ className }) => (
  <Icon icon="fe:question" className={className} style={{ color: 'grey' }} />
);

export default QuestionIcon;
